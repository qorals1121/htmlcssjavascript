<script> 태그는 <body>태그가 닫히기 전에 사용

자바스크립트에서 배열은 대괄호로 묶어서 바로 사용한다. ["값1", "값2" ,,,]
비교연산자는 자바와 조금 차이가 있다.

attribute = 속성
getAttribute = 속성값 가져옴
setAttribute = 속성값 설정

url = 포트 번호까지 같이 찍히는 주소
uri = 포트 번호 뒤에 찍히는 주소

제이슨은 데이터만 전송할때 사용을 많이한다.
그리고 함수를 다시 살릴 수 없다.